// services/Contact/ContactService.spec.ts

import ContactService from './ContactService';

describe('ContactService', () => {
  const component = ContactService;

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
