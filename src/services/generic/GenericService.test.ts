// GenericService.spec.ts

import { GenericService } from './GenericService';

describe('GenericService', () => {
  const component = GenericService;

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
