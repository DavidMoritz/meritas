// InputRadioPillGroup.spec.ts

import InputRadioGroup from './InputRadioGroup';

describe('<InputRadioPillGroup />', () => {
  const component = InputRadioGroup;

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
