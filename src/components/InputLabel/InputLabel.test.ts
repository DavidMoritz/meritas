// InputLabel.spec.ts

import InputLabel from './InputLabel';

describe('<InputLabel />', () => {
  const component = InputLabel;

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
