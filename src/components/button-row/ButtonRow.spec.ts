// ButtonRow.spec.ts

import ButtonRow from './ButtonRow';

describe('<ButtonRow />', () => {
  const component = ButtonRow;

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
