// forms/ContactLog/ContactLogForm.spec.ts

import ContactForm from './ContactForm.tsx';

describe('<ContactLogForm />', () => {
  const component = ContactForm;

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
